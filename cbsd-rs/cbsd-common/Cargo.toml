[package]
name = "cbsd-common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared utilities for cbsd-server and cbsd-worker (no IO, no async)"

[dependencies]
