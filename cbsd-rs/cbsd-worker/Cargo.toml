[package]
name = "cbsd-worker"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "CBS build service daemon — worker binary"

[dependencies]
cbsd-proto = { path = "../cbsd-proto" }
