# Generated with Claude Sonnet 4.6
#   by Joao Eduardo Luis <joao@clyso.com> on Mar 31 2026
#
name: Release cbsd-rs

on:
  push:
    tags:
      - "v*"

  workflow_dispatch:
    inputs:
      tag_name:
        description: "Tag to build (e.g., v1.0.0)"
        required: true
        type: string

jobs:
  build-cbc:
    uses: ./.github/workflows/build-cbc.yaml
    with:
      build_tag: ${{ inputs.tag_name || github.ref_name }}

  build-release-bundle:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          ref: ${{ inputs.tag_name }}

      - name: Build Config Tarball
        run: |
          ./cbsd-rs/systemd/gen-config-archive.sh

      - name: Create Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          tag_name: ${{ inputs.tag_name }}
          files: |
            cbsd-rs-config.tar
            cbsd-rs/systemd/install.sh
          generate_release_notes: true
